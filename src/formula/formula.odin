package formula

Formula :: struct {
    name:          string,
    desc:          string,
    version:       string,
    bottle_url:    string,
    bottle_sha256: string,
    source_url:    string,
    source_sha256: string,
}
